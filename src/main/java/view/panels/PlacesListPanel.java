package view.panels;

import view.color.Colors;

import javax.swing.*;
import java.awt.*;

public class PlacesListPanel extends JPanel {
    public PlacesListPanel() {
        setBackground(Colors.BACKGROUND);
        setLayout(new BorderLayout());
    }
}
